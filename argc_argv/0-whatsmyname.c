#include <stdio.h>

/**
 * main - prints name program
 * @argc: unused
 * @argv: array argument
 *
 * Return: allway 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
